# AGENTS.md — DSH 工作区指令

> 这个文件由 DSH 自动读取，用来告诉 Agent 这个工作区的规矩。
> 位置：`E:\DSH\workspace\AGENTS.md`

## 关于我

中文回答。主人是学生，方向是网络工程 / CTF / 运维，平时用我写代码、整理资料、记录学习笔记。

## GitHub 仓库

主人的笔记与项目仓库：

| 项目 | 值 |
|---|---|
| 仓库 | `hwr2938189618-blip/Rong` |
| 默认分支 | `main` |
| 可见性 | 公开 |
| 权限 | 可读写（token 有 repo 权限） |
| 用途 | 代码项目 + 学习笔记知识库 |

目录结构：

```
Rong/
├─ README.md
├─ notes/                    学习笔记（主要工作区）
│  ├─ README.md              分类导航 + 笔记索引，【新增笔记后必须更新】
│  ├─ _templates/
│  │  └─ note-template.md    笔记模板
│  ├─ toolbox/               通用工具与环境排错
│  ├─ windows/               Windows 系统操作
│  └─ dsh/                   DSH 机制与排错
├─ skills/                   DSH 原生技能镜像（索引 skills/README.md）
└─ projects/                 代码项目
```

> 技能的真源在 `E:\DSH\home\skills\`（DSH 的 user-dsh 扫描根），仓库里的是镜像与备份。

## 学到新东西就往仓库里记（默认执行，不必等主人开口）

主人 2026-09-16 定的规矩：**新知识不能只留在本地或对话里**。学到/整理出新东西时，按载体分工自动落地：

| 内容类型 | 存哪 | 是否进仓库 |
|---|---|---|
| 短事实 / 偏好 / 决策 / 踩坑 | 记忆库 `memory_save` | — |
| 成套流程 / 方法论 / 命令模板 | 技能 `E:\DSH\home\skills\<name>\SKILL.md` | ✅ 镜像到 `skills/<name>/` |
| 大块资料 / 教程 / 代码片段 | 笔记 | ✅ 提交到 `notes/<分类>/` |
| 必须遵守的规矩 | 本文件 AGENTS.md | ✅ 一并提交 |

流程：

1. 归类 → 写笔记或技能（技能同时写进工作区镜像 `E:\DSH\workspace\skills\`）
2. **更新 `notes/README.md` 导航**（分类计数 +1、索引加一行）
3. 用 MCP 一次性提交（`push_files` 一次 = 一个 commit），提交信息中文
4. 回读验证：commit 存在 + 原文件未丢 + 字节数与本地一致

**仍需先问的边界**：凭据/密钥、隐私内容、覆盖或删除主人已有内容。
**注意**：写 `E:\DSH\home\skills` 在工作区之外，沙箱会拦 → 需要一次性提权。

## 提交到哪里（本机唯一可行路径）

`gh` CLI 从未登录，且 `github.com` 主站不通（21 秒超时）→ **不要试 `gh auth login`**。
一律走 MCP：`mcp__github__push_files` / `get_file_contents` / `list_commits`（走 `api.github.com`，实测可用）。

## ⚠️ 这个仓库不是我一个人在用（三个 AI 共写）

同一个仓库 `hwr2938189618-blip/Rong`，本机还有别的 AI 助手在读写在写。**改共享文件前必须先拉最新版。**

| 助手 | 推送通道 | 状态 |
|---|---|---|
| **DSH**（我） | `mcp__github__push_files` / `create_or_update_file` | 在用 |
| **WorkBuddy** | REST API 手工六步（技能 `rong-notes-publish` + `publish.py`） | 在用 |
| **Codex** | 待主人安排 | ⬜ 尚未接入（主人说「以后有空也会弄」） |

### 防覆盖铁律（必须遵守）

1. **推送前先 `get_file_contents` 读一次目标文件的最新版** —— 不要用自己缓存的旧版本整体覆盖
2. **共享文件是高频冲突点**：`notes/README.md`、根 `README.md`、`AGENTS.md` —— 改之前必拉最新
3. 推完**回读验证**：分类计数与实际文件数是否一致、有没有把别人的改动覆盖掉
4. 学别的 AI 的做法时**注意路径归属**：各家的技能目录不同，别照抄（如 WorkBuddy 的技能在 `~/.workbuddy/skills/`，Codex 的配置在 `D:\codex\.codex\`）

> 2026-09-16 实测：我和 WorkBuddy 在同一分钟内各推了一次 `notes/README.md`，**这次没丢内容**（它后发、以我的版本为底座叠加），但那是运气不是机制。参与者变多，冲突只会更多。

## 凭据

GitHub 令牌通过环境变量 **`GITHUB_PAT_TOKEN`** 提供，已配置在 MCP 服务器里。

**⛔ 绝对不要做的事**：不要把令牌打印出来、不要写进任何文件、不要提交进仓库。

## 记笔记的规范（重要）

主人说「记一篇笔记」时，按下面的流程走：

### 1. 先读模板和导航

- 模板：`notes/_templates/note-template.md`
- 导航：`notes/README.md`（看现有分类，决定新笔记放哪）

### 2. 笔记格式（严格照模板）

```markdown
# [主题] [标题]

> **日期**: YYYY-MM-DD
> **标签**: #标签1 #标签2
> **状态**: 🟡 草稿

## 我要解决什么问题
## 核心概念
## 示例代码
## 踩坑记录     ← 表格形式：问题 / 原因 / 解决
## 参考链接
## 回顾记录     ← 每次改动追加一条
```

状态只有两种，写的时候用对：

- 🟡 **草稿** — 刚记下，未整理
- 🟢 **已整理** — 结构完整，可放心回顾

### 3. 命名与归类

- 文件名：`<序号>-<主题>.md`，例如 `notes/toolbox/02-git-基础.md`
- 按主题归入 `notes/<分类>/`；没有合适分类就新建一个目录
- 一篇笔记只讲一个主题，**宁短勿杂**
- 踩坑记录特别宝贵，必须写清「问题 / 原因 / 解决」

### 4. 更新导航（容易漏，务必做）

写完笔记后，同步更新 `notes/README.md`：

- 「分类导航」表里对应分类的**笔记数 +1**（分类是新的话先插一行）
- 「笔记索引」表里加一行：笔记链接 / 主题 / 状态

### 5. 提交

写完后用 GitHub 工具提交到 `main`，提交信息用中文，格式：

```
笔记: 新增《标题》
```

或更新时：

```
笔记: 更新《标题》- 补充 XX
```

## 其他约定

- 改动他人仓库前先确认分支和内容，别覆盖主人的东西
- `projects/` 下是代码，改之前先说明改什么
- 不确定的事直接问，不要猜
